export interface GitReleaseInfo {
  commitHash: string;
  commitShort: string;
  commitDate: string;
  commitMsg: string;
  commitAuthor: string;
  branch: string;
  repoUrl: string;
  commitUrl: string;
}
